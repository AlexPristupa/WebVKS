export enum PaginationPosition {
  top = 'top',
  bottom = 'bottom',
}

export enum PaginationLayout {
  unknown = 'prev, pager, next, jumper, ->, total, slot',
  top = 'sizes',
  bottom = 'prev, pager, next, jumper',
}
