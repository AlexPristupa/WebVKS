/**
 * @description Параметры для функции задержки
 *              в общем случае выбирать "slow"
 */
export const debounce = {
  timeOut: {
    fastest: 100,
    fast: 300,
    medium: 600,
    slow: 900,
    slowest: 1000,
  },
  immediate: false,
}
